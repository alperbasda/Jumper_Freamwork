
//---------------------------------------------------------------------------------------
//      This code was generated by a Jumper tool. 
//      Runtime version : 1.0
//      Generation Time : 23.10.2023 11:28
//---------------------------------------------------------------------------------------

using AutoMapper;
using IdentityServer.Application.Features.AuthMessageses.Commands.BulkUpdate;
using IdentityServer.Application.Features.AuthMessageses.Rules;
using IdentityServer.Application.Services.Repositories;
using IdentityServer.Domain.Entities;
using MediatR;
namespace IdentityServer.Application.Features.AuthMessageses.Handlers.Commands.BulkUpdate;

public class BulkUpdateAuthMessagesCommandHandler : IRequestHandler<BulkUpdateAuthMessagesWrapperCommand, List<BulkUpdateAuthMessagesResponse>>
{
    private readonly IAuthMessagesDal _authMessagesDal;
    private readonly AuthMessagesBusinessRules _authMessagesBusinessRules;
    private readonly IMapper _mapper;

    public BulkUpdateAuthMessagesCommandHandler(IMapper mapper, IAuthMessagesDal authMessagesDal, AuthMessagesBusinessRules authMessagesBusinessRules)
    {
        _mapper = mapper;
        _authMessagesDal = authMessagesDal;
        _authMessagesBusinessRules = authMessagesBusinessRules;
    }

    public async Task<List<BulkUpdateAuthMessagesResponse>> Handle(BulkUpdateAuthMessagesWrapperCommand request, CancellationToken cancellationToken)
    {
        var ids = request.Items.Select(q => q.Id).ToList();
        var datas = await _authMessagesDal.GetListAsync(w => ids.Contains(w.Id), cancellationToken : cancellationToken);

        _authMessagesBusinessRules.ThrowExceptionIfDataNull(datas);
        
        //İş Kurallarınızı Burada Çağırabilirsiniz.
        foreach(var item in datas.Items)
        {
             var selectedRequest = request.Items.FirstOrDefault(w => w.Id == item.Id);
             if (selectedRequest == null)
             {
                continue;
             }
            _mapper.Map(selectedRequest, item);
        }
        
        await _authMessagesDal.UpdateRangeAsync(datas.Items);

        return _mapper.Map<List<BulkUpdateAuthMessagesResponse>>(datas.Items);
    }
}



