@using Core.Persistence.Models.Responses;
@using Jumper.Application.Features.PropertyTypeDeclarations.Queries.GetAllFromCache;
@model List<GetAllFromCachePropertyTypeDeclarationResponse>

@{
    SelectBoxModel selected = (SelectBoxModel)ViewData["SelectBoxModel"]!;
}
<div class="form-floating">
    <select class="form-control form-control-sm" placeholder="Nesne Seçin" name="@selected.Name" style="width:100%">
        <option value="@null">Seçim Yapın</option>
        @foreach (var item in Model)
        {
            if (selected.SelectedValue == item.Id.ToString())
            {
                <option selected value="@item.Code">@item.Name</option>
            }
            else
            {
                <option value="@item.Code">@item.Name</option>
            }

        }
    </select>

    <label class="form-label mr-4" for="@selected.Name"> Tip </label>
</div>